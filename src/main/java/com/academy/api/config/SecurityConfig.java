package com.academy.api.config;

import org.springframework.context.annotation.Configuration;

/**
 * 기존 SecurityConfig는 새로운 JWT 기반 보안 설정으로 대체되었습니다.
 * 
 * @see com.academy.api.auth.security.SecurityConfiguration
 */
@Configuration
public class SecurityConfig {
    // 이 클래스는 JWT 기반 보안 설정으로 대체되어 더 이상 사용되지 않습니다.
    // JWT 인증 설정은 SecurityConfiguration 클래스를 참조하세요.
}